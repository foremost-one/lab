server:
  port: 8080

spring:
  main:
    banner-mode: CONSOLE

logging:
  file: ./logs/customers.log
  level:
    org.springframework.web: INFO
    one.foremost.lab.customer: INFO
        
app:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/customerdb
    username: postgres
    password: password
    pool-size: 30
